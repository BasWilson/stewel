{
    "hosts": [
        {
            "host": "shadow.adraptor.network",
            "targets": ["http://localhost:4000"],
            "loadBalancer": {
                "method": "round-robin"
            }
        }
    ]
}
