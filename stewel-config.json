{
    "hosts": [
        {
            "host": "yourhost.com",
            "targets": [
                "http://localhost:4000",
                "http://localhost:4001"
            ],
            "loadBalancer": {
                "method": "round-robin"
            }
        }
    ]
}